// Given p, g, h, find x in the h = g^x in Zp*
const DiscreteLogSolver = {
  solve: function (p: bigint, g: bigint, h: bigint): bigint {
    p;
    g;
    h;
    return 1n;
  },
};

export default DiscreteLogSolver;
